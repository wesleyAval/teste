#include <iostream>
using namespace std;

int num4(){
	return 7;
}

int num3(){
	return 3;
}

int num2(){
	return 2;
}

int num(){
	return 10;
}

int main(){
	int num;
	num =0;
	num=9;

	cout << "CONSEGUI PORRA";
	cout << "blabla";
	cin >> num;
}

//git checkout -- arquivo.ext -> desfaz as alterações realizadas desde o último commit(antes do add)
//git reset arquivo.ext -> tira o arquivo do stage
//git reset cod -> desfaz os commits até o commit correspondente até ao código, mas não altera os arquivos
//git reset --hard HEAD~1 -> desfaz o último commit, alterando os arquivos para o que estavam antes
//git revert cod -> reverte as alterações realizadas no commit correspondente ao código
//git stash -> guarda as atualizações que ainda não foram comitadas